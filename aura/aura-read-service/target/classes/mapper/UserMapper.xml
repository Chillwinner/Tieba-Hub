<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aura.mapper.UserMapper">

    <insert id="insert">
        INSERT INTO user_table (id, phone, password, nickname, email, create_time)
        VALUES (#{id}, #{phone}, #{password}, #{nickname}, #{email}, #{createTime})
    </insert>

    <select id="findById" resultType="com.aura.entity.User">
        SELECT * FROM user_table WHERE id = #{id}
    </select>

    <select id="findByPhone" resultType="com.aura.entity.User">
        SELECT * FROM user_table WHERE phone = #{phone}
    </select>

    <select id="findAll" resultType="com.aura.entity.User">
        SELECT * FROM user_table ORDER BY create_time DESC
    </select>

    <update id="update">
        UPDATE user_table
        SET nickname = #{nickname}, email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user_table WHERE id = #{id}
    </delete>

</mapper>
